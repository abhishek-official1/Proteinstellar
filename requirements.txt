# Core Dependencies
numpy>=1.21.0,<2.0.0
biopython>=1.79
ipython>=7.0.0
ipywidgets>=7.6.0

# Jupyter Notebook
jupyter>=1.0.0
notebook>=6.4.0

# Visualization
matplotlib>=3.3.0
py3Dmol>=2.0.0

# AlphaFold/ColabFold (for local installation)
# Note: For Google Colab, these are installed automatically in the notebook
# colabfold[alphafold] @ git+https://github.com/sokrypton/ColabFold
# jax[cuda]>=0.3.25  # For CUDA support
# dm-haiku==0.0.10
# dm-tree>=0.1.6
# tensorflow>=2.12.0

# Molecular Simulation (optional, for AMBER relaxation)
# openmm>=7.7.0  # Requires conda installation
# pdbfixer>=1.8.1  # Requires conda installation

# File handling
pydrive2>=1.15.0  # For Google Drive integration

# Development tools (optional)
black>=22.0.0
flake8>=4.0.0
isort>=5.10.0

# Scientific computing
scipy>=1.7.0
pandas>=1.3.0

# Network requests
requests>=2.26.0
urllib3>=1.26.0

# Testing (optional)
pytest>=7.0.0
pytest-cov>=3.0.0

# Additional bioinformatics tools
# Note: Some tools require separate installation:
# - DSSP: apt install dssp (Linux) or brew install dssp (macOS)
# - HHsuite: conda install -c bioconda hhsuite
# - MMseqs2: Available via web API (no local install needed)
# - BLAST+: conda install -c bioconda blast
